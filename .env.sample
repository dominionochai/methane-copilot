# Sample environment file for marss2l.
#
# Copy this file to `.env`, fill in real values, and load it into your shell
# (e.g. `set -a; source .env; set +a`) before running training, inference,
# or the notebook / integration tests. ALL credentials are read from the
# environment — never drop secret files into the package directory.
#
# In CI these are provided through GitHub Actions secrets. The notebook and
# train integration tests are skipped unless the env vars they depend on are
# set (see tests/conftest.py).
#
# The values below are MOCKS — replace them with your own.

# --- Weights & Biases ----------------
WANDB_API_KEY=00000000000000000000000000000000
WANDB_PROJECT=s2l89-model

# --- Google Earth Engine ----------------
# EARTHENGINE_SERVICE_ACCOUNT_KEY holds the full service-account JSON as a
# single line.
EARTHENGINE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"my-project","private_key_id":"abc123","private_key":"-----BEGIN PRIVATE KEY-----\nMOCK\n-----END PRIVATE KEY-----\n","client_email":"mars-s2l@my-project.iam.gserviceaccount.com","client_id":"123456789","token_uri":"https://oauth2.googleapis.com/token"}
# EARTHENGINE_PROJECT is optional (Earth Engine is initialised with
# project=None if unset).
EARTHENGINE_PROJECT=my-gee-project

# --- Azure blob storage ----------------
AZURE_STORAGE_ACCOUNT=mystorageaccount
CONTAINER_NAME=my-container
SAS_TOKEN=sv=2022-11-02&ss=b&srt=co&sp=rl&se=2030-01-01T00:00:00Z&st=2024-01-01T00:00:00Z&spr=https&sig=MOCKSIGNATUREVALUE

# The MARS-S2L Hugging Face dataset is public, so no token is required to
# read it.

# Methane Copilot runtime variables
EIA_API_KEY=
COPILOT_PLUME_JSON=
COPILOT_FACILITIES_JSON=
COPILOT_CASE_DIR=copilot/cases
COPILOT_CREDIT_DIR=copilot/credits
